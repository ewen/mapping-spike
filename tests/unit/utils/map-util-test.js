import mapUtil from 'mapping-spike/utils/map-util';
import { module, test } from 'qunit';

module('Unit | Utility | map util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mapUtil();
  assert.ok(result);
});
